import React from 'react';
import { SelectAndGetCircuitSimulation } from './SelectAndGetCircuitSimulation';

export default {
    title: 'Slect and get circuit simulation',
    component: SelectAndGetCircuitSimulation,
};

export const Default = () => {

    return <SelectAndGetCircuitSimulation />
}